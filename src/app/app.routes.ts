import { Routes } from '@angular/router';
import { MainFormComponent } from './invoice/main-form/main-form.component';
import { InvoiceComponent } from './invoice/invoice.component';

export const routes: Routes = [{ path: '', component: InvoiceComponent }];
